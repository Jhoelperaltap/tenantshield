"""Framework adapters for TenantShield.

This package contains optional integrations with external frameworks.
Each adapter is opt-in via extras (e.g. ``pip install tenantshield[django]``).

Currently shipped:

- :mod:`tenantshield.adapters.django` -- Django ORM enforcement.

Coming in future phases: SQLAlchemy (Phase 3), Celery (Phase 4), DRF (Sub-phase 2C).
"""
